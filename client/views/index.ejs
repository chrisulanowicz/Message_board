<!DOCTYPE html>
<html>
<head>
	<title>Coding Dojo Message Board</title>
	<link rel="stylesheet" href="/style.css" />
</head>
<body>
	<div id="container">
		<h1>The Dojo Message Board</h1>
		<form id="add_post" action="/add_post" method="post">
			<label for="name">Name: </label>
			<input type="text" id="name" name="name" />
			<label for="message">Message: </label>
			<textarea id="message" name="message" cols="100%" rows="6"></textarea>
			<input class="submit" type="submit" value="Post Message" />
		</form>
		<% if(typeof(errors) != 'undefined'){ %>
			<% for(var x in errors){ %>
				<h3><%= errors[x].message %></h3>
			<% } %>
		<% } %>
		<% if(typeof(posts) != 'undefined'){%>
			<% for(var x in posts){ %>
		<div class="post">
			<h3>Name: <%= posts[x].name %></h3>
			<h3>Message: <%= posts[x].message %></h3>
			<div class="comments">
				<% for(var y in posts[x].comments){ %>
					<p class="inner_comments">Name: <%= posts[x].comments[y].name %></p>
					<p>Comment: <%= posts[x].comments[y].comment %></p>
				<% } %>
				<form class="comments" action="/add_comment/<%= posts[x].id %>" method="post">
					<label for="name">Name: </label>
					<input type="text" id="name" name="name" />
					<label for="comment">Comment: </label>
					<textarea id="comment" name="comment" cols="80%" rows="4"></textarea>
					<input class="submit" type="submit" value="Post Comment" />
				</form>
			</div>
		</div>
			<% } %>
		<% } %>
	</div>
</body>
</html>